apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: svc-trilium
labels:
  - pairs:
      app: trilium
      gitops: flux
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - pvc.yaml