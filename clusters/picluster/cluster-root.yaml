apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-root
  namespace: flux-system
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  path: ./apps
  wait: false      # <- wichtig: kein Warten auf Ready
  prune: true
  commonMetadata:
    labels: { gitops: flux }
  decryption:
    provider: sops
    secretRef:
      name: sops-age

